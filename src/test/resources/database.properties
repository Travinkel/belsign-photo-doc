# Test Database Configuration (SQLite)
db.url=jdbc:sqlite:memory:belsign_test
db.username=
db.password=
db.driver=org.sqlite.JDBC
# Connection Pool Configuration
db.pool.maxSize=10
db.pool.minIdle=5
db.pool.idleTimeout=30000
db.pool.connectionTimeout=30000